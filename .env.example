# Environment variables for the SQLite management system.
# Copy this file to .env and fill in the values.
# Currently no ENV variables are required.
# Per-environment configuration is defined in src/config.py.
